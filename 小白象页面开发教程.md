# 公共配置页面完整开发教程

## 以“设备台账管理”为例，严格按照你截图中的实际开发方式

这次我不再使用通用化的 `@action='add'`、`@edit1` 新增模式，而是按照你截图中这套系统的真实规则来讲：

```
主页面查询条件      → @edit1、@edit2……
新增/修改/删除窗口  → @clyj1、@clyj2……
当前右键操作名称    → @clyj
当前行主键          → @pkey1
页面查询类型        → @ggmk_lx='cx'或'mx'
动态路由类型        → action='cx'、'mx'、'cz'
```

你截图中实际采用的是两个业务存储过程：

```
p_equipment_cx
├─ @ggmk_lx='cx'：查询设备主页面
└─ @ggmk_lx='mx'：查询设备操作记录

p_equipment_cz
├─ @clyj='新增设备'
├─ @clyj='修改设备'
└─ @clyj='删除设备'
```

完整调用关系：

```
base_ggmkmain
    ↓
创建“设备台账管理”页面

base_ggmkdetail
    ↓
配置查询区、新增窗口、修改窗口、删除窗口

base_ggmkright
    ↓
配置查看操作记录、修改、删除、设备标签管理

t_ggmk_routes
    ↓
cx → p_equipment_cx
mx → p_equipment_cx
cz → p_equipment_cz

业务存储过程
    ↓
读取@edit、@clyj、@clyjN、@pkeyN完成业务
```

------

# 第一部分：先理解参数到底怎么传

这是这套系统最关键的部分。

## 1. 查询页面使用 `@editN`

设备主页面的查询区有七个条件：

| 查询条件 | 参数     |
| -------- | -------- |
| 设备编号 | `@edit1` |
| 设备名称 | `@edit2` |
| 型号规格 | `@edit3` |
| 使用部门 | `@edit4` |
| 负责人   | `@edit5` |
| 设备状态 | `@edit6` |
| 记录状态 | `@edit7` |

因此查询存储过程里写：

```
ISNULL(@edit1, '') = ''
OR equipment_code LIKE '%' + @edit1 + '%'
```

------

## 2. 新增窗口使用 `@clyj1～@clyj9`

虽然这些字段在页面上也是普通文本框，但你这套公共程序打开新增、修改窗口以后，传给业务过程的是：

```
@clyj1
@clyj2
@clyj3
……
```

新增设备的对应关系：

| 新增字段 | 参数     |
| -------- | -------- |
| 设备编号 | `@clyj1` |
| 设备名称 | `@clyj2` |
| 型号规格 | `@clyj3` |
| 使用部门 | `@clyj4` |
| 负责人   | `@clyj5` |
| 购置日期 | `@clyj6` |
| 设备状态 | `@clyj7` |
| 设备价格 | `@clyj8` |
| 设备备注 | `@clyj9` |

截图中的新增判断正是：

```
IF @clyj = N'新增设备'
BEGIN
    IF ISNULL(TRIM(@clyj1), '') = ''
    BEGIN
        SET @flag_sm = N'新增失败：设备编号不能为空！';
        RETURN;
    END;
END;
```

这里：

```
@clyj = 当前执行的操作名称
@clyj1 = 新增窗口第一个控件的值
```

不要把二者混淆。

------

## 3. 修改窗口也使用 `@clyjN`

修改窗口没有再让用户编辑设备编号，设备编号来自：

```
@pkey1
```

修改字段对应关系：

| 修改字段     | 参数     |
| ------------ | -------- |
| 当前设备编号 | `@pkey1` |
| 设备名称     | `@clyj1` |
| 型号规格     | `@clyj2` |
| 使用部门     | `@clyj3` |
| 负责人       | `@clyj4` |
| 购置日期     | `@clyj5` |
| 设备状态     | `@clyj6` |
| 设备价格     | `@clyj7` |
| 设备备注     | `@clyj8` |
| 修改原因     | `@clyj9` |

判断：

```
IF @clyj = N'修改设备'
BEGIN
    -- 使用@pkey1定位设备
    -- 使用@clyj1～@clyj9读取修改窗口内容
END;
```

------

## 4. 删除窗口

删除时只需要：

```
@pkey1 = 当前设备编号
@clyj1 = 删除原因
@clyj  = 删除设备
```

判断：

```
IF @clyj = N'删除设备'
BEGIN
    IF ISNULL(@pkey1, '') = ''
    BEGIN
        SET @flag_sm = N'删除失败：没有获取到设备编号。';
        RETURN;
    END;

    IF ISNULL(@clyj1, '') = ''
    BEGIN
        SET @flag_sm = N'删除失败：请填写删除原因。';
        RETURN;
    END;
END;
```

------

# 第二部分：创建业务表

## 1. 创建设备主表

```
USE TEST;
GO

IF OBJECT_ID('dbo.t_equipment', 'U') IS NOT NULL
BEGIN
    DROP TABLE dbo.t_equipment;
END;
GO

CREATE TABLE dbo.t_equipment
(
    iid INT IDENTITY(1,1) NOT NULL,

    equipment_code VARCHAR(30) NOT NULL,
    equipment_name NVARCHAR(100) NOT NULL,

    model_spec NVARCHAR(50) NOT NULL
        CONSTRAINT DF_t_equipment_model_spec DEFAULT(N''),

    department_name NVARCHAR(100) NOT NULL,
    
    keeper_name NVARCHAR(100) NOT NULL
        CONSTRAINT DF_t_equipment_keeper_name DEFAULT(N''),

    purchase_date DATE NULL,

    equipment_status NVARCHAR(20) NOT NULL,

    equipment_price DECIMAL(18,2) NOT NULL
        CONSTRAINT DF_t_equipment_price DEFAULT(0),

    equipment_remark NVARCHAR(1000) NOT NULL
        CONSTRAINT DF_t_equipment_remark DEFAULT(N''),

    flag_zf SMALLINT NOT NULL
        CONSTRAINT DF_t_equipment_flag_zf DEFAULT(0),

    bz_zf NVARCHAR(500) NOT NULL
        CONSTRAINT DF_t_equipment_bz_zf DEFAULT(N''),

    czydm_dj VARCHAR(20) NOT NULL,
    czyxm_dj NVARCHAR(50) NOT NULL,
    time_dj DATETIME NOT NULL,

    CONSTRAINT PK_t_equipment
        PRIMARY KEY(equipment_code)
);
GO
```

字段说明：

| 字段               | 含义                 |
| ------------------ | -------------------- |
| `equipment_code`   | 设备编号             |
| `equipment_name`   | 设备名称             |
| `model_spec`       | 型号规格             |
| `department_name`  | 使用部门             |
| `keeper_name`      | 负责人               |
| `purchase_date`    | 购置日期             |
| `equipment_status` | 使用中、维修中、停用 |
| `equipment_price`  | 设备价格             |
| `equipment_remark` | 备注                 |
| `flag_zf`          | 0正常，1已删除       |
| `bz_zf`            | 删除原因             |
| `czydm_dj`         | 最后登记人工号       |
| `czyxm_dj`         | 最后登记人姓名       |
| `time_dj`          | 最后登记时间         |

------

## 2. 创建操作记录表

```
USE TEST;
GO

IF OBJECT_ID('dbo.t_equipment_backup', 'U') IS NOT NULL
BEGIN
    DROP TABLE dbo.t_equipment_backup;
END;
GO

CREATE TABLE dbo.t_equipment_backup
(
    backup_iid INT IDENTITY(1,1) NOT NULL
        CONSTRAINT PK_t_equipment_backup PRIMARY KEY,

    source_iid INT NOT NULL,

    equipment_code VARCHAR(30) NOT NULL,
    equipment_name NVARCHAR(100) NOT NULL,
    model_spec NVARCHAR(50) NOT NULL,
    department_name NVARCHAR(100) NOT NULL,
    keeper_name NVARCHAR(100) NOT NULL,
    purchase_date DATE NULL,
    equipment_status NVARCHAR(20) NOT NULL,
    equipment_price DECIMAL(18,2) NOT NULL,
    equipment_remark NVARCHAR(1000) NOT NULL,

    flag_zf SMALLINT NOT NULL,
    bz_zf NVARCHAR(500) NOT NULL,

    czydm_dj VARCHAR(20) NOT NULL,
    czyxm_dj NVARCHAR(50) NOT NULL,
    time_dj DATETIME NOT NULL,

    czlx_backup NVARCHAR(20) NOT NULL,
    czydm_backup VARCHAR(20) NOT NULL,
    czyxm_backup NVARCHAR(50) NOT NULL,
    time_backup DATETIME NOT NULL,

    sm_backup NVARCHAR(2000) NOT NULL
        CONSTRAINT DF_t_equipment_backup_sm DEFAULT(N'')
);
GO
```

操作记录表不仅记录谁进行了操作，还保存操作完成时设备的完整快照。

------

## 3. 插入测试数据

```
USE TEST;
GO

INSERT INTO dbo.t_equipment
(
    equipment_code,
    equipment_name,
    model_spec,
    department_name,
    keeper_name,
    purchase_date,
    equipment_status,
    equipment_price,
    equipment_remark,

    flag_zf,
    bz_zf,

    czydm_dj,
    czyxm_dj,
    time_dj
)
VALUES
(
    'EQ-TEST',
    N'测试设备',
    N'Model-01',
    N'测试部门',
    N'张三',
    '2026-07-01',
    N'使用中',
    10000,
    N'页面开发测试数据',

    0,
    N'',

    '1561',
    N'测试用户',
    GETDATE()
);
GO
```

检查：

```
SELECT *
FROM TEST.dbo.t_equipment;
```

------

# 第三部分：创建查询存储过程

根据截图，查询和操作记录查询都写在：

```
p_equipment_cx
```

中，通过：

```
@ggmk_lx
```

区分。

```
@ggmk_lx='cx' → 查询设备主页面
@ggmk_lx='mx' → 查询设备操作记录
```

------

## 1. 标准参数头

这套公共平台在调用路由过程时会传很多通用参数。业务过程必须接住这些参数，否则可能出现：

```
某某参数不是存储过程的参数
```

建议直接复制你们现有模板的完整参数头。

下面按照截图整理：

```
USE TEST;
GO

CREATE OR ALTER PROCEDURE dbo.p_equipment_cx
    @ggmk_lx NVARCHAR(50) = N'',
    @action NVARCHAR(50) = N'',
    @gzfs NVARCHAR(50) = N'',
    @rcaption NVARCHAR(50) = N'',

    @clyj NVARCHAR(50) = N'',
    @clfs NVARCHAR(50) = N'',

    @flag_sm NVARCHAR(500) = N'' OUTPUT,

    @czydm VARCHAR(20) = '',
    @czyxm NVARCHAR(50) = N'',

    @mkmc VARCHAR(50) = '',

    @qxxssp VARCHAR(8000) = '',
    @yhrybm VARCHAR(8000) = '',
    @qxxsdw VARCHAR(8000) = '',

    @lx NVARCHAR(50) = N'',
    @tjtj VARCHAR(8000) = '',
    @gsywbm VARCHAR(8000) = '',

    @string1 VARCHAR(8000) = '',
    @string2 NVARCHAR(1000) = N'',
    @string3 VARCHAR(1000) = '',
    @string4 VARCHAR(1000) = '',
    @bmdr VARCHAR(1000) = '',

    @pkey1 NVARCHAR(50) = N'',
    @pkey2 NVARCHAR(50) = N'',
    @pkey3 NVARCHAR(50) = N'',
    @pkey4 NVARCHAR(50) = N'',
    @pkey5 NVARCHAR(50) = N'',
    @pkey6 NVARCHAR(50) = N'',
    @pkey7 NVARCHAR(50) = N'',
    @pkey8 NVARCHAR(50) = N'',
    @pkey9 NVARCHAR(50) = N'',
    @pkey10 NVARCHAR(50) = N'',

    @edit1 NVARCHAR(50) = N'',
    @edit2 NVARCHAR(50) = N'',
    @edit3 NVARCHAR(50) = N'',
    @edit4 NVARCHAR(50) = N'',
    @edit5 NVARCHAR(50) = N'',
    @edit6 NVARCHAR(50) = N'',
    @edit7 NVARCHAR(50) = N'',
    @edit8 NVARCHAR(50) = N'',
    @edit9 NVARCHAR(50) = N'',
    @edit10 NVARCHAR(50) = N'',
    @edit11 NVARCHAR(50) = N'',
    @edit12 NVARCHAR(50) = N'',
    @edit13 NVARCHAR(50) = N'',
    @edit14 NVARCHAR(50) = N'',
    @edit15 NVARCHAR(50) = N'',
    @edit16 NVARCHAR(50) = N'',
    @edit17 NVARCHAR(50) = N'',
    @edit18 NVARCHAR(50) = N'',
    @edit19 NVARCHAR(50) = N'',
    @edit20 NVARCHAR(50) = N'',

    @clyj1 NVARCHAR(500) = N'',
    @clyj2 NVARCHAR(500) = N'',
    @clyj3 NVARCHAR(500) = N'',
    @clyj4 NVARCHAR(500) = N'',
    @clyj5 NVARCHAR(500) = N'',
    @clyj6 NVARCHAR(500) = N'',
    @clyj7 NVARCHAR(500) = N'',
    @clyj8 NVARCHAR(500) = N'',
    @clyj9 NVARCHAR(500) = N'',
    @clyj10 NVARCHAR(500) = N'',
    @clyj11 NVARCHAR(500) = N'',
    @clyj12 NVARCHAR(500) = N'',
    @clyj13 NVARCHAR(500) = N'',
    @clyj14 NVARCHAR(500) = N'',
    @clyj15 NVARCHAR(500) = N'',
    @clyj16 NVARCHAR(500) = N'',
    @clyj17 NVARCHAR(500) = N'',
    @clyj18 NVARCHAR(500) = N'',
    @clyj19 NVARCHAR(500) = N'',
    @clyj20 NVARCHAR(500) = N'',

    @dwdm_fgs NVARCHAR(50) = N'',
    @qx NVARCHAR(50) = N'',
    @cxtj NVARCHAR(50) = N'',
    @jlssm NVARCHAR(50) = N'',
    @ckdm NVARCHAR(50) = N'',

    @dgvindex NVARCHAR(50) = N'',

    @sm NVARCHAR(500) = N'' OUTPUT,
    @clbz NVARCHAR(50) = N'',
    @is_confirm NVARCHAR(50) = N''
AS
BEGIN
    SET NOCOUNT ON;

    SET @flag_sm = N'';
```

------

## 2. 主页面查询部分

接在上面过程内部：

```
    /*====================================================
      一、查询设备主页面
    ====================================================*/
    IF @ggmk_lx = N'cx'
    BEGIN
        SELECT
            a.equipment_code AS [设备编号],
            a.equipment_name AS [设备名称],
            a.model_spec AS [型号规格],
            a.department_name AS [使用部门],
            a.keeper_name AS [负责人],

            CONVERT(VARCHAR(10), a.purchase_date, 120)
                AS [购置时间],

            a.equipment_status AS [设备状态],
            a.equipment_price AS [设备价格],
            a.equipment_remark AS [设备备注],

            CASE a.flag_zf
                WHEN 1 THEN N'已删除'
                ELSE N'正常'
            END AS [记录状态],

            a.bz_zf AS [删除原因],

            a.czydm_dj AS [登记人工号],
            a.czyxm_dj AS [登记人姓名],

            CONVERT(VARCHAR(19), a.time_dj, 120)
                AS [登记时间]

        FROM dbo.t_equipment a WITH(NOLOCK)

        WHERE
            (
                ISNULL(@edit1, N'') = N''
                OR a.equipment_code LIKE '%' + @edit1 + '%'
            )

            AND
            (
                ISNULL(@edit2, N'') = N''
                OR a.equipment_name LIKE N'%' + @edit2 + N'%'
            )

            AND
            (
                ISNULL(@edit3, N'') = N''
                OR a.model_spec LIKE N'%' + @edit3 + N'%'
            )

            AND
            (
                ISNULL(@edit4, N'') = N''
                OR a.department_name = @edit4
            )

            AND
            (
                ISNULL(@edit5, N'') = N''
                OR a.keeper_name LIKE N'%' + @edit5 + N'%'
            )

            AND
            (
                ISNULL(@edit6, N'') = N''
                OR a.equipment_status = @edit6
            )

            AND
            (
                (
                    ISNULL(@edit7, N'') IN (N'', N'正常')
                    AND a.flag_zf = 0
                )
                OR
                (
                    @edit7 = N'已删除'
                    AND a.flag_zf = 1
                )
                OR
                (
                    @edit7 = N'全部'
                )
            )

        ORDER BY a.equipment_code;

        RETURN;
    END;
```

------

## 3. 操作记录查询部分

截图中的 `@ggmk_lx='mx'` 查询的是备份表，而不是设备主表单条详情。

```
    /*====================================================
      二、查询设备操作记录
    ====================================================*/
    IF @ggmk_lx = N'mx'
    BEGIN
        SELECT
            b.backup_iid AS [操作记录编号],
            b.czlx_backup AS [操作类型],

            b.equipment_code AS [设备编号],
            b.equipment_name AS [设备名称],
            b.model_spec AS [型号规格],
            b.department_name AS [使用部门],
            b.keeper_name AS [负责人],

            CONVERT(VARCHAR(10), b.purchase_date, 120)
                AS [购置时间],

            b.equipment_status AS [设备状态],
            b.equipment_price AS [设备价格],
            b.equipment_remark AS [设备备注],

            CASE b.flag_zf
                WHEN 1 THEN N'已删除'
                ELSE N'正常'
            END AS [操作后状态],

            b.bz_zf AS [删除原因],

            b.czydm_backup AS [操作人工号],
            b.czyxm_backup AS [操作人姓名],

            CONVERT(VARCHAR(19), b.time_backup, 120)
                AS [操作时间],

            b.sm_backup AS [操作说明]

        FROM dbo.t_equipment_backup b WITH(NOLOCK)

        WHERE
            (
                ISNULL(@pkey1, N'') = N''
                OR b.equipment_code = @pkey1
            )

        ORDER BY
            b.time_backup DESC,
            b.backup_iid DESC;

        RETURN;
    END;

    SET @flag_sm =
        N'未识别的查询类型：'
        + ISNULL(@ggmk_lx, N'NULL');
END;
GO
```

------

# 第四部分：创建新增、修改、删除存储过程

业务操作使用：

```
p_equipment_cz
```

操作类型不再根据 `@action` 判断，而是根据：

```
@clyj
```

判断。

```
@clyj='新增设备'
@clyj='修改设备'
@clyj='删除设备'
```

------

## 1. 创建标准过程头

参数头与查询过程保持一致。实际使用时，可以直接复制 `p_equipment_cx` 的参数头，只把过程名改成：

```
dbo.p_equipment_cz
```

下面从过程主体开始说明：

```
USE TEST;
GO

CREATE OR ALTER PROCEDURE dbo.p_equipment_cz
    @ggmk_lx NVARCHAR(50) = N'',
    @action NVARCHAR(50) = N'',
    @gzfs NVARCHAR(50) = N'',
    @rcaption NVARCHAR(50) = N'',

    @clyj NVARCHAR(50) = N'',
    @clfs NVARCHAR(50) = N'',

    @flag_sm NVARCHAR(500) = N'' OUTPUT,

    @czydm VARCHAR(20) = '',
    @czyxm NVARCHAR(50) = N'',

    @mkmc VARCHAR(50) = '',
    @qxxssp VARCHAR(8000) = '',
    @yhrybm VARCHAR(8000) = '',
    @qxxsdw VARCHAR(8000) = '',

    @lx NVARCHAR(50) = N'',
    @tjtj VARCHAR(8000) = '',
    @gsywbm VARCHAR(8000) = '',

    @string1 VARCHAR(8000) = '',
    @string2 NVARCHAR(1000) = N'',
    @string3 VARCHAR(1000) = '',
    @string4 VARCHAR(1000) = '',
    @bmdr VARCHAR(1000) = '',

    @pkey1 NVARCHAR(50) = N'',
    @pkey2 NVARCHAR(50) = N'',
    @pkey3 NVARCHAR(50) = N'',
    @pkey4 NVARCHAR(50) = N'',
    @pkey5 NVARCHAR(50) = N'',
    @pkey6 NVARCHAR(50) = N'',
    @pkey7 NVARCHAR(50) = N'',
    @pkey8 NVARCHAR(50) = N'',
    @pkey9 NVARCHAR(50) = N'',
    @pkey10 NVARCHAR(50) = N'',

    @edit1 NVARCHAR(50) = N'',
    @edit2 NVARCHAR(50) = N'',
    @edit3 NVARCHAR(50) = N'',
    @edit4 NVARCHAR(50) = N'',
    @edit5 NVARCHAR(50) = N'',
    @edit6 NVARCHAR(50) = N'',
    @edit7 NVARCHAR(50) = N'',
    @edit8 NVARCHAR(50) = N'',
    @edit9 NVARCHAR(50) = N'',
    @edit10 NVARCHAR(50) = N'',
    @edit11 NVARCHAR(50) = N'',
    @edit12 NVARCHAR(50) = N'',
    @edit13 NVARCHAR(50) = N'',
    @edit14 NVARCHAR(50) = N'',
    @edit15 NVARCHAR(50) = N'',
    @edit16 NVARCHAR(50) = N'',
    @edit17 NVARCHAR(50) = N'',
    @edit18 NVARCHAR(50) = N'',
    @edit19 NVARCHAR(50) = N'',
    @edit20 NVARCHAR(50) = N'',

    @clyj1 NVARCHAR(500) = N'',
    @clyj2 NVARCHAR(500) = N'',
    @clyj3 NVARCHAR(500) = N'',
    @clyj4 NVARCHAR(500) = N'',
    @clyj5 NVARCHAR(500) = N'',
    @clyj6 NVARCHAR(500) = N'',
    @clyj7 NVARCHAR(500) = N'',
    @clyj8 NVARCHAR(500) = N'',
    @clyj9 NVARCHAR(500) = N'',
    @clyj10 NVARCHAR(500) = N'',
    @clyj11 NVARCHAR(500) = N'',
    @clyj12 NVARCHAR(500) = N'',
    @clyj13 NVARCHAR(500) = N'',
    @clyj14 NVARCHAR(500) = N'',
    @clyj15 NVARCHAR(500) = N'',
    @clyj16 NVARCHAR(500) = N'',
    @clyj17 NVARCHAR(500) = N'',
    @clyj18 NVARCHAR(500) = N'',
    @clyj19 NVARCHAR(500) = N'',
    @clyj20 NVARCHAR(500) = N'',

    @dwdm_fgs NVARCHAR(50) = N'',
    @qx NVARCHAR(50) = N'',
    @cxtj NVARCHAR(50) = N'',
    @jlssm NVARCHAR(50) = N'',
    @ckdm NVARCHAR(50) = N'',

    @dgvindex NVARCHAR(50) = N'',

    @sm NVARCHAR(500) = N'' OUTPUT,
    @clbz NVARCHAR(50) = N'',
    @is_confirm NVARCHAR(50) = N''
AS
BEGIN
    SET NOCOUNT ON;
    SET XACT_ABORT ON;

    DECLARE @now DATETIME = GETDATE();

    SET @flag_sm = N'';
```

------

## 2. 新增设备

新增窗口参数：

```
@clyj1 = 设备编号
@clyj2 = 设备名称
@clyj3 = 型号规格
@clyj4 = 使用部门
@clyj5 = 负责人
@clyj6 = 购置日期
@clyj7 = 设备状态
@clyj8 = 设备价格
@clyj9 = 设备备注
```

过程代码：

```
    /*====================================================
      一、新增设备
    ====================================================*/
    IF @clyj = N'新增设备'
    BEGIN
        DECLARE @add_date DATE;
        DECLARE @add_price DECIMAL(18,2);

        IF ISNULL(LTRIM(RTRIM(@clyj1)), N'') = N''
        BEGIN
            SET @flag_sm = N'新增失败：设备编号不能为空！';
            RETURN;
        END;

        IF ISNULL(LTRIM(RTRIM(@clyj2)), N'') = N''
        BEGIN
            SET @flag_sm = N'新增失败：设备名称不能为空！';
            RETURN;
        END;

        IF ISNULL(LTRIM(RTRIM(@clyj4)), N'') = N''
        BEGIN
            SET @flag_sm = N'新增失败：使用部门不能为空！';
            RETURN;
        END;

        IF ISNULL(LTRIM(RTRIM(@clyj7)), N'') = N''
        BEGIN
            SET @flag_sm = N'新增失败：设备状态不能为空！';
            RETURN;
        END;

        IF EXISTS
        (
            SELECT 1
            FROM dbo.t_equipment
            WHERE equipment_code = @clyj1
        )
        BEGIN
            SET @flag_sm =
                N'新增失败：设备编号【'
                + @clyj1
                + N'】已经存在！';

            RETURN;
        END;

        IF ISNULL(@clyj6, N'') <> N''
           AND TRY_CONVERT(DATE, @clyj6, 120) IS NULL
        BEGIN
            SET @flag_sm =
                N'新增失败：购置日期格式不正确，应为yyyy-MM-dd。';

            RETURN;
        END;

        IF ISNULL(@clyj8, N'') <> N''
           AND TRY_CONVERT(DECIMAL(18,2), @clyj8) IS NULL
        BEGIN
            SET @flag_sm = N'新增失败：设备价格格式不正确。';
            RETURN;
        END;

        SET @add_date =
            TRY_CONVERT(DATE, NULLIF(@clyj6, N''), 120);

        SET @add_price =
            ISNULL
            (
                TRY_CONVERT(DECIMAL(18,2), @clyj8),
                0
            );

        BEGIN TRY
            BEGIN TRAN;

            INSERT INTO dbo.t_equipment
            (
                equipment_code,
                equipment_name,
                model_spec,
                department_name,
                keeper_name,
                purchase_date,
                equipment_status,
                equipment_price,
                equipment_remark,

                flag_zf,
                bz_zf,

                czydm_dj,
                czyxm_dj,
                time_dj
            )
            VALUES
            (
                @clyj1,
                @clyj2,
                ISNULL(@clyj3, N''),
                @clyj4,
                ISNULL(@clyj5, N''),
                @add_date,
                @clyj7,
                @add_price,
                ISNULL(@clyj9, N''),

                0,
                N'',

                @czydm,
                @czyxm,
                @now
            );

            INSERT INTO dbo.t_equipment_backup
            (
                source_iid,

                equipment_code,
                equipment_name,
                model_spec,
                department_name,
                keeper_name,
                purchase_date,
                equipment_status,
                equipment_price,
                equipment_remark,

                flag_zf,
                bz_zf,

                czydm_dj,
                czyxm_dj,
                time_dj,

                czlx_backup,
                czydm_backup,
                czyxm_backup,
                time_backup,
                sm_backup
            )
            SELECT
                a.iid,

                a.equipment_code,
                a.equipment_name,
                a.model_spec,
                a.department_name,
                a.keeper_name,
                a.purchase_date,
                a.equipment_status,
                a.equipment_price,
                a.equipment_remark,

                a.flag_zf,
                a.bz_zf,

                a.czydm_dj,
                a.czyxm_dj,
                a.time_dj,

                N'新增',
                @czydm,
                @czyxm,
                @now,
                N'新增设备'
            FROM dbo.t_equipment a
            WHERE a.equipment_code = @clyj1;

            COMMIT TRAN;

            SET @flag_sm = N'新增设备成功！';
            RETURN;
        END TRY
        BEGIN CATCH
            IF @@TRANCOUNT > 0
                ROLLBACK TRAN;

            SET @flag_sm =
                N'新增失败：'
                + ERROR_MESSAGE();

            RETURN;
        END CATCH;
    END;
```

------

## 3. 修改设备

修改设备时，先读取旧值，然后将页面传入的新值转换为最终值。

截图中的修改规则是：

```
页面不填写某一项 → 保留旧值
页面填写某一项   → 使用新值
```

代码：

```
    /*====================================================
      二、修改设备
    ====================================================*/
    IF @clyj = N'修改设备'
    BEGIN
        DECLARE
            @old_name NVARCHAR(100),
            @old_model NVARCHAR(50),
            @old_department NVARCHAR(100),
            @old_keeper NVARCHAR(100),
            @old_date DATE,
            @old_status NVARCHAR(20),
            @old_price DECIMAL(18,2),
            @old_remark NVARCHAR(1000);

        DECLARE
            @new_name NVARCHAR(100),
            @new_model NVARCHAR(50),
            @new_department NVARCHAR(100),
            @new_keeper NVARCHAR(100),
            @new_date DATE,
            @new_status NVARCHAR(20),
            @new_price DECIMAL(18,2),
            @new_remark NVARCHAR(1000),
            @change_text NVARCHAR(2000);

        IF ISNULL(@pkey1, N'') = N''
        BEGIN
            SET @flag_sm = N'修改失败：没有获取到设备编号。';
            RETURN;
        END;

        IF NOT EXISTS
        (
            SELECT 1
            FROM dbo.t_equipment
            WHERE equipment_code = @pkey1
              AND flag_zf = 0
        )
        BEGIN
            SET @flag_sm = N'修改失败：设备不存在或已删除。';
            RETURN;
        END;

        IF ISNULL(@clyj9, N'') = N''
        BEGIN
            SET @flag_sm = N'修改失败：请填写修改原因。';
            RETURN;
        END;

        SELECT
            @old_name = equipment_name,
            @old_model = model_spec,
            @old_department = department_name,
            @old_keeper = keeper_name,
            @old_date = purchase_date,
            @old_status = equipment_status,
            @old_price = equipment_price,
            @old_remark = equipment_remark
        FROM dbo.t_equipment
        WHERE equipment_code = @pkey1;

        SET @new_name =
            CASE
                WHEN ISNULL(@clyj1, N'') = N''
                    THEN @old_name
                ELSE @clyj1
            END;

        SET @new_model =
            CASE
                WHEN ISNULL(@clyj2, N'') = N''
                    THEN @old_model
                ELSE @clyj2
            END;

        SET @new_department =
            CASE
                WHEN ISNULL(@clyj3, N'') = N''
                    THEN @old_department
                ELSE @clyj3
            END;

        SET @new_keeper =
            CASE
                WHEN ISNULL(@clyj4, N'') = N''
                    THEN @old_keeper
                ELSE @clyj4
            END;

        SET @new_date =
            CASE
                WHEN ISNULL(@clyj5, N'') = N''
                    THEN @old_date
                ELSE TRY_CONVERT(DATE, @clyj5, 120)
            END;

        SET @new_status =
            CASE
                WHEN ISNULL(@clyj6, N'') = N''
                    THEN @old_status
                ELSE @clyj6
            END;

        SET @new_price =
            CASE
                WHEN ISNULL(@clyj7, N'') = N''
                    THEN @old_price
                ELSE TRY_CONVERT(DECIMAL(18,2), @clyj7)
            END;

        SET @new_remark =
            CASE
                WHEN ISNULL(@clyj8, N'') = N''
                    THEN @old_remark
                ELSE @clyj8
            END;

        IF ISNULL(@clyj5, N'') <> N''
           AND @new_date IS NULL
        BEGIN
            SET @flag_sm =
                N'修改失败：购置日期格式不正确，应为yyyy-MM-dd。';

            RETURN;
        END;

        IF ISNULL(@clyj7, N'') <> N''
           AND @new_price IS NULL
        BEGIN
            SET @flag_sm = N'修改失败：设备价格格式不正确。';
            RETURN;
        END;

        SET @change_text = N'';

        IF ISNULL(@old_name, N'') <> ISNULL(@new_name, N'')
        BEGIN
            SET @change_text +=
                N'设备名称由【'
                + ISNULL(@old_name, N'')
                + N'】修改为【'
                + ISNULL(@new_name, N'')
                + N'】；';
        END;

        IF ISNULL(@old_model, N'') <> ISNULL(@new_model, N'')
        BEGIN
            SET @change_text +=
                N'型号规格由【'
                + ISNULL(@old_model, N'')
                + N'】修改为【'
                + ISNULL(@new_model, N'')
                + N'】；';
        END;

        IF ISNULL(@old_department, N'')
           <> ISNULL(@new_department, N'')
        BEGIN
            SET @change_text +=
                N'使用部门由【'
                + ISNULL(@old_department, N'')
                + N'】修改为【'
                + ISNULL(@new_department, N'')
                + N'】；';
        END;

        IF ISNULL(@old_keeper, N'')
           <> ISNULL(@new_keeper, N'')
        BEGIN
            SET @change_text +=
                N'负责人由【'
                + ISNULL(@old_keeper, N'')
                + N'】修改为【'
                + ISNULL(@new_keeper, N'')
                + N'】；';
        END;

        IF ISNULL(CONVERT(VARCHAR(10), @old_date, 120), '')
           <> ISNULL(CONVERT(VARCHAR(10), @new_date, 120), '')
        BEGIN
            SET @change_text +=
                N'购置日期由【'
                + ISNULL(CONVERT(VARCHAR(10), @old_date, 120), '')
                + N'】修改为【'
                + ISNULL(CONVERT(VARCHAR(10), @new_date, 120), '')
                + N'】；';
        END;

        IF ISNULL(@old_status, N'')
           <> ISNULL(@new_status, N'')
        BEGIN
            SET @change_text +=
                N'设备状态由【'
                + ISNULL(@old_status, N'')
                + N'】修改为【'
                + ISNULL(@new_status, N'')
                + N'】；';
        END;

        IF ISNULL(@old_price, 0)
           <> ISNULL(@new_price, 0)
        BEGIN
            SET @change_text +=
                N'设备价格由【'
                + CONVERT(NVARCHAR(50), ISNULL(@old_price, 0))
                + N'】修改为【'
                + CONVERT(NVARCHAR(50), ISNULL(@new_price, 0))
                + N'】；';
        END;

        IF ISNULL(@old_remark, N'')
           <> ISNULL(@new_remark, N'')
        BEGIN
            SET @change_text +=
                N'设备备注由【'
                + ISNULL(@old_remark, N'')
                + N'】修改为【'
                + ISNULL(@new_remark, N'')
                + N'】；';
        END;

        IF ISNULL(@change_text, N'') = N''
        BEGIN
            SET @flag_sm = N'修改失败：没有检测到字段变化。';
            RETURN;
        END;

        BEGIN TRY
            BEGIN TRAN;

            UPDATE dbo.t_equipment
            SET
                equipment_name = @new_name,
                model_spec = @new_model,
                department_name = @new_department,
                keeper_name = @new_keeper,
                purchase_date = @new_date,
                equipment_status = @new_status,
                equipment_price = @new_price,
                equipment_remark = @new_remark,

                czydm_dj = @czydm,
                czyxm_dj = @czyxm,
                time_dj = @now
            WHERE equipment_code = @pkey1
              AND flag_zf = 0;

            INSERT INTO dbo.t_equipment_backup
            (
                source_iid,

                equipment_code,
                equipment_name,
                model_spec,
                department_name,
                keeper_name,
                purchase_date,
                equipment_status,
                equipment_price,
                equipment_remark,

                flag_zf,
                bz_zf,

                czydm_dj,
                czyxm_dj,
                time_dj,

                czlx_backup,
                czydm_backup,
                czyxm_backup,
                time_backup,
                sm_backup
            )
            SELECT
                a.iid,

                a.equipment_code,
                a.equipment_name,
                a.model_spec,
                a.department_name,
                a.keeper_name,
                a.purchase_date,
                a.equipment_status,
                a.equipment_price,
                a.equipment_remark,

                a.flag_zf,
                a.bz_zf,

                a.czydm_dj,
                a.czyxm_dj,
                a.time_dj,

                N'修改',
                @czydm,
                @czyxm,
                @now,

                N'修改原因：'
                + @clyj9
                + N'；变更内容：'
                + @change_text
            FROM dbo.t_equipment a
            WHERE a.equipment_code = @pkey1;

            COMMIT TRAN;

            SET @flag_sm = N'修改设备成功！';
            RETURN;
        END TRY
        BEGIN CATCH
            IF @@TRANCOUNT > 0
                ROLLBACK TRAN;

            SET @flag_sm =
                N'修改失败：'
                + ERROR_MESSAGE();

            RETURN;
        END CATCH;
    END;
```

------

## 4. 删除设备

```
    /*====================================================
      三、删除设备
    ====================================================*/
    IF @clyj = N'删除设备'
    BEGIN
        IF ISNULL(@pkey1, N'') = N''
        BEGIN
            SET @flag_sm = N'删除失败：没有获取到设备编号。';
            RETURN;
        END;

        IF ISNULL(@clyj1, N'') = N''
        BEGIN
            SET @flag_sm = N'删除失败：请填写删除原因。';
            RETURN;
        END;

        IF NOT EXISTS
        (
            SELECT 1
            FROM dbo.t_equipment
            WHERE equipment_code = @pkey1
              AND flag_zf = 0
        )
        BEGIN
            SET @flag_sm = N'删除失败：设备不存在或已经删除。';
            RETURN;
        END;

        BEGIN TRY
            BEGIN TRAN;

            UPDATE dbo.t_equipment
            SET
                flag_zf = 1,
                bz_zf = @clyj1,

                czydm_dj = @czydm,
                czyxm_dj = @czyxm,
                time_dj = @now
            WHERE equipment_code = @pkey1
              AND flag_zf = 0;

            INSERT INTO dbo.t_equipment_backup
            (
                source_iid,

                equipment_code,
                equipment_name,
                model_spec,
                department_name,
                keeper_name,
                purchase_date,
                equipment_status,
                equipment_price,
                equipment_remark,

                flag_zf,
                bz_zf,

                czydm_dj,
                czyxm_dj,
                time_dj,

                czlx_backup,
                czydm_backup,
                czyxm_backup,
                time_backup,
                sm_backup
            )
            SELECT
                a.iid,

                a.equipment_code,
                a.equipment_name,
                a.model_spec,
                a.department_name,
                a.keeper_name,
                a.purchase_date,
                a.equipment_status,
                a.equipment_price,
                a.equipment_remark,

                a.flag_zf,
                a.bz_zf,

                a.czydm_dj,
                a.czyxm_dj,
                a.time_dj,

                N'删除',
                @czydm,
                @czyxm,
                @now,

                N'删除原因：'
                + @clyj1
            FROM dbo.t_equipment a
            WHERE a.equipment_code = @pkey1;

            COMMIT TRAN;

            SET @flag_sm = N'删除设备成功！';
            RETURN;
        END TRY
        BEGIN CATCH
            IF @@TRANCOUNT > 0
                ROLLBACK TRAN;

            SET @flag_sm =
                N'删除失败：'
                + ERROR_MESSAGE();

            RETURN;
        END CATCH;
    END;

    SET @flag_sm =
        N'未识别的操作名称：'
        + ISNULL(@clyj, N'NULL');
END;
GO
```

------

# 第五部分：配置 `base_ggmkmain` 主页面

页面名称：

```
设备台账管理
```

主键显示列：

```
设备编号
```

根据截图，配置方式为：

```
USE PUBLIC_CS;
GO

DECLARE @lx NVARCHAR(50) = N'设备台账管理';
DECLARE @iid INT;

SELECT @iid = iid
FROM dbo.base_ggmkmain
WHERE lx = @lx;

PRINT @iid;
```

如果页面不存在，再插入：

```
IF @iid IS NULL
BEGIN
    INSERT INTO dbo.base_ggmkmain
    (
        lx,
        flag_qx,
        flag_yx,
        pkey,
        mkmc,
        djsj,
        mxbz,
        xzbz,
        mkmcstr,
        excel
    )
    VALUES
    (
        N'设备台账管理',
        0,
        1,
        N'设备编号',
        N'vw',
        GETDATE(),
        1,
        1,
        N'',
        N''
    );
END;
GO
```

再查询：

```
SELECT *
FROM PUBLIC_CS.dbo.base_ggmkmain
WHERE lx = N'设备台账管理';
```

最关键的是：

```
lx   = 设备台账管理
pkey = 设备编号
```

查询过程必须返回：

```
equipment_code AS [设备编号]
```

否则右键主键取不到。

------

# 第六部分：配置主页面查询区

先取得页面编号：

```
USE PUBLIC_CS;
GO

DECLARE @iid INT;

SELECT @iid = iid
FROM dbo.base_ggmkmain WITH(NOLOCK)
WHERE lx = N'设备台账管理';

SELECT @iid AS 页面编号;
```

查询控件：

| xh   | 名称     | 参数     |
| ---- | -------- | -------- |
| 1    | 设备编号 | `@edit1` |
| 2    | 设备名称 | `@edit2` |
| 3    | 型号规格 | `@edit3` |
| 4    | 使用部门 | `@edit4` |
| 5    | 负责人   | `@edit5` |
| 6    | 设备状态 | `@edit6` |
| 7    | 记录状态 | `@edit7` |
| 88   | 查询按钮 | 执行查询 |

配置时：

```
lx='cx'
rid=0
```

你已有完整的 `base_ggmkdetail` 列清单，因此保留原来的列清单，只替换核心值。

示意：

```
-- 设备编号
SELECT
    iid = @iid,
    xh = 1,
    labelname = 'label1',
    labelcaption = N'设备编号',
    comboboxname = 'TextBox1',
    sqlstr = '',
    lx = 'cx',
    rid = 0,
    controltype = 'TextBox';

-- 设备名称
SELECT
    iid = @iid,
    xh = 2,
    labelcaption = N'设备名称',
    comboboxname = 'TextBox1',
    sqlstr = '',
    lx = 'cx',
    rid = 0,
    controltype = 'TextBox';
```

使用部门下拉内容示例：

```
SELECT N'' AS tj
UNION ALL
SELECT N'测试部门'
UNION ALL
SELECT N'智能运营部';
```

设备状态：

```
SELECT N'' AS tj
UNION ALL
SELECT N'使用中'
UNION ALL
SELECT N'维修中'
UNION ALL
SELECT N'停用';
```

记录状态：

```
SELECT N'正常' AS tj
UNION ALL
SELECT N'已删除'
UNION ALL
SELECT N'全部';
```

查询按钮：

```
xh=88
labelcaption=查询
lx=cx
controltype=Button
```

配置完成后检查：

```
SELECT *
FROM PUBLIC_CS.dbo.base_ggmkdetail
WHERE iid = @iid
  AND lx = 'cx'
ORDER BY xh;
```

------

# 第七部分：配置新增窗口

新增窗口：

```
lx='xz'
rid=0
```

字段顺序：

| xh   | 字段     | 业务过程参数 |
| ---- | -------- | ------------ |
| 10   | 设备编号 | `@clyj1`     |
| 20   | 设备名称 | `@clyj2`     |
| 30   | 型号规格 | `@clyj3`     |
| 40   | 使用部门 | `@clyj4`     |
| 50   | 负责人   | `@clyj5`     |
| 60   | 购置日期 | `@clyj6`     |
| 70   | 设备状态 | `@clyj7`     |
| 80   | 设备价格 | `@clyj8`     |
| 90   | 设备备注 | `@clyj9`     |

注意：

> `xh` 是10、20、30，但传参仍然按照控件顺序进入 `@clyj1、@clyj2、@clyj3`，不是 `@clyj10、@clyj20`。

截图中控件类型：

```
设备编号   TextBox
设备名称   TextBox
型号规格   TextBox
使用部门   下拉框
负责人     TextBox
购置日期   DateTime
设备状态   下拉框
设备价格   TextBox
设备备注   RichTextBox
```

配置完成检查：

```
SELECT *
FROM PUBLIC_CS.dbo.base_ggmkdetail
WHERE iid = @iid
  AND lx = 'xz'
ORDER BY xh;
```

------

# 第八部分：配置右键菜单

根据截图，右键菜单至少包含：

```
查看操作记录
修改设备
删除设备
设备标签管理
```

先取页面编号：

```
DECLARE @iid INT;

SELECT @iid = iid
FROM PUBLIC_CS.dbo.base_ggmkmain
WHERE lx = N'设备台账管理';
```

------

## 1. 查看操作记录

核心配置：

```
rcaption = 查看操作记录
lx       = 8
pkey     = 设备编号
```

查询过程将进入：

```
@ggmk_lx='mx'
```

并通过：

```
@pkey1
```

取得当前设备编号。

------

## 2. 修改设备

核心配置：

```
rcaption = 修改设备
clyj     = 修改设备
lx       = 2
pkey     = 设备编号
```

其中：

```
base_ggmkright.clyj='修改设备'
```

最终传入业务过程：

```
@clyj = N'修改设备'
```

右键菜单的 `cid` 将作为修改窗口控件的：

```
rid
```

------

## 3. 删除设备

核心配置：

```
rcaption = 删除设备
clyj     = 删除设备
lx       = 2
pkey     = 设备编号
```

最终业务过程进入：

```
IF @clyj = N'删除设备'
```

------

## 4. `pxs` 字段

截图中修改菜单的 `pxs` 设置了当前行字段，例如：

```
设备编号,
设备名称,
型号规格,
使用部门,
负责人,
购置时间,
设备状态,
设备价格,
设备备注,
记录状态
```

该配置用于告诉公共页面，从当前表格行中获取哪些值。

设置示例：

```
UPDATE PUBLIC_CS.dbo.base_ggmkright
SET pxs =
    N'设备编号,设备名称,型号规格,使用部门,负责人,'
    + N'购置时间,设备状态,设备价格,设备备注,记录状态'
WHERE iid = @iid
  AND rcaption = N'修改设备';
```

------

# 第九部分：配置修改窗口

先取得修改右键菜单的 `cid`：

```
DECLARE @iid INT;
DECLARE @rid_update INT;

SELECT @iid = iid
FROM PUBLIC_CS.dbo.base_ggmkmain
WHERE lx = N'设备台账管理';

SELECT @rid_update = cid
FROM PUBLIC_CS.dbo.base_ggmkright
WHERE iid = @iid
  AND rcaption = N'修改设备';

SELECT
    @iid AS 页面编号,
    @rid_update AS 修改菜单编号;
```

修改窗口配置：

```
lx='r'
rid=@rid_update
```

字段：

| xh   | 字段     | 参数     |
| ---- | -------- | -------- |
| 1    | 设备名称 | `@clyj1` |
| 2    | 型号规格 | `@clyj2` |
| 3    | 使用部门 | `@clyj3` |
| 4    | 负责人   | `@clyj4` |
| 5    | 购置日期 | `@clyj5` |
| 6    | 设备状态 | `@clyj6` |
| 7    | 设备价格 | `@clyj7` |
| 8    | 设备备注 | `@clyj8` |
| 9    | 修改原因 | `@clyj9` |

为什么修改窗口不配置设备编号？

因为设备编号已经通过当前行主键传给：

```
@pkey1
```

因此修改时不能再依赖 `@clyj1` 找设备。

检查：

```
SELECT *
FROM PUBLIC_CS.dbo.base_ggmkdetail
WHERE iid = @iid
  AND lx = 'r'
  AND rid = @rid_update
ORDER BY xh;
```

------

# 第十部分：配置删除窗口

先取删除菜单编号：

```
DECLARE @rid_delete INT;

SELECT @rid_delete = cid
FROM PUBLIC_CS.dbo.base_ggmkright
WHERE iid = @iid
  AND rcaption = N'删除设备';
```

删除窗口只有一个字段：

| xh   | 字段     | 参数     |
| ---- | -------- | -------- |
| 1    | 删除原因 | `@clyj1` |

配置：

```
lx='r'
rid=@rid_delete
controltype=TextBox或RichTextBox
```

检查：

```
SELECT *
FROM PUBLIC_CS.dbo.base_ggmkdetail
WHERE iid = @iid
  AND lx = 'r'
  AND rid = @rid_delete
ORDER BY xh;
```

------

# 第十一部分：配置动态路由

根据实际存储过程分工，配置三条路由：

| action | 存储过程                  | 功能             |
| ------ | ------------------------- | ---------------- |
| `cx`   | `TEST.dbo.p_equipment_cx` | 查询主页面       |
| `mx`   | `TEST.dbo.p_equipment_cx` | 查询操作记录     |
| `cz`   | `TEST.dbo.p_equipment_cz` | 新增、修改、删除 |

配置：

```
USE PUBLIC_CS;
GO

DECLARE @lx NVARCHAR(50) = N'设备台账管理';
DECLARE @max_iid INT;

SELECT @max_iid = ISNULL(MAX(iid), 0)
FROM dbo.t_ggmk_routes;

DELETE FROM dbo.t_ggmk_routes
WHERE lx = @lx
  AND action IN ('cx', 'mx', 'cz');

INSERT INTO dbo.t_ggmk_routes
(
    iid,
    lx,
    pname,
    action,
    jmlx,
    czydm_dj,
    czyxm_dj,
    djsj
)
VALUES
(
    @max_iid + 1,
    @lx,
    N'TEST.dbo.p_equipment_cx',
    'cx',
    N'普通界面',
    '1561',
    N'测试用户',
    GETDATE()
),
(
    @max_iid + 2,
    @lx,
    N'TEST.dbo.p_equipment_cx',
    'mx',
    N'普通界面',
    '1561',
    N'测试用户',
    GETDATE()
),
(
    @max_iid + 3,
    @lx,
    N'TEST.dbo.p_equipment_cz',
    'cz',
    N'普通界面',
    '1561',
    N'测试用户',
    GETDATE()
);
GO
```

检查：

```
SELECT
    lx,
    pname,
    action,
    jmlx
FROM PUBLIC_CS.dbo.t_ggmk_routes
WHERE lx = N'设备台账管理'
ORDER BY action;
```

------

# 第十二部分：增加右键上下选入口

已有设备标签上下选页面时，只需要在设备主页面增加右键菜单。

核心配置：

```
菜单名称：设备标签管理
lx=6
pkey=设备编号
toform=form_common_comment
```

目标页面：

```
<Data>
    <key lx="设备标签上下选" constr="constrxt" />
</Data>
```

将当前行设备编号传给 `@args1`：

```
<Data>
    <key lx="1" value="设备编号" />
</Data>
```

因此核心配置为：

```
rcaption     = 设备标签管理
lx           = 6
pkey         = 设备编号
toform       = form_common_comment
toformarg    = <Data><key lx="设备标签上下选" constr="constrxt" /></Data>
toformargxml = <Data><key lx="1" value="设备编号" /></Data>
```

上下选路由：

```
INSERT INTO PUBLIC_CS.dbo.t_ggmk_routes
(
    iid,
    lx,
    pname,
    action,
    jmlx,
    czydm_dj,
    czyxm_dj,
    djsj
)
VALUES
(
    业务可用编号1,
    N'设备标签上下选',
    N'TEST.dbo.p_equipment_tag_query',
    'cx',
    N'多选界面',
    '1561',
    N'测试用户',
    GETDATE()
),
(
    业务可用编号2,
    N'设备标签上下选',
    N'TEST.dbo.p_equipment_tag_modify',
    'cz',
    N'多选界面',
    '1561',
    N'测试用户',
    GETDATE()
);
```

------

# 第十三部分：手工测试存储过程

## 1. 测试查询

```
USE TEST;
GO

DECLARE @flag_sm NVARCHAR(500) = N'';
DECLARE @sm NVARCHAR(500) = N'';

EXEC dbo.p_equipment_cx
    @ggmk_lx = N'cx',
    @edit1 = N'',
    @edit2 = N'',
    @edit3 = N'',
    @edit4 = N'',
    @edit5 = N'',
    @edit6 = N'',
    @edit7 = N'正常',

    @czydm = '1561',
    @czyxm = N'测试用户',

    @flag_sm = @flag_sm OUTPUT,
    @sm = @sm OUTPUT;

SELECT
    @flag_sm AS 提示信息,
    @sm AS 其他信息;
```

------

## 2. 测试新增

```
DECLARE @flag_sm NVARCHAR(500) = N'';
DECLARE @sm NVARCHAR(500) = N'';

EXEC dbo.p_equipment_cz
    @clyj = N'新增设备',

    @clyj1 = N'EQ-002',
    @clyj2 = N'第二台测试设备',
    @clyj3 = N'Model-02',
    @clyj4 = N'测试部门',
    @clyj5 = N'李四',
    @clyj6 = N'2026-07-10',
    @clyj7 = N'使用中',
    @clyj8 = N'18000.50',
    @clyj9 = N'新增功能测试',

    @czydm = '1561',
    @czyxm = N'测试用户',

    @flag_sm = @flag_sm OUTPUT,
    @sm = @sm OUTPUT;

SELECT @flag_sm AS 提示信息;
```

------

## 3. 测试修改

```
DECLARE @flag_sm NVARCHAR(500) = N'';
DECLARE @sm NVARCHAR(500) = N'';

EXEC dbo.p_equipment_cz
    @clyj = N'修改设备',

    @pkey1 = N'EQ-002',

    @clyj1 = N'第二台测试设备修改版',
    @clyj2 = N'Model-02A',
    @clyj3 = N'智能运营部',
    @clyj4 = N'王五',
    @clyj5 = N'2026-07-11',
    @clyj6 = N'维修中',
    @clyj7 = N'20000',
    @clyj8 = N'修改测试备注',
    @clyj9 = N'测试修改设备信息',

    @czydm = '1561',
    @czyxm = N'测试用户',

    @flag_sm = @flag_sm OUTPUT,
    @sm = @sm OUTPUT;

SELECT @flag_sm AS 提示信息;
```

------

## 4. 测试删除

```
DECLARE @flag_sm NVARCHAR(500) = N'';
DECLARE @sm NVARCHAR(500) = N'';

EXEC dbo.p_equipment_cz
    @clyj = N'删除设备',

    @pkey1 = N'EQ-002',
    @clyj1 = N'测试逻辑删除',

    @czydm = '1561',
    @czyxm = N'测试用户',

    @flag_sm = @flag_sm OUTPUT,
    @sm = @sm OUTPUT;

SELECT @flag_sm AS 提示信息;
```

------

## 5. 测试操作记录

```
DECLARE @flag_sm NVARCHAR(500) = N'';
DECLARE @sm NVARCHAR(500) = N'';

EXEC dbo.p_equipment_cx
    @ggmk_lx = N'mx',
    @pkey1 = N'EQ-002',

    @czydm = '1561',
    @czyxm = N'测试用户',

    @flag_sm = @flag_sm OUTPUT,
    @sm = @sm OUTPUT;

SELECT @flag_sm AS 提示信息;
```

------

# 第十四部分：完整页面调用流程

## 1. 查询

```
用户打开设备台账管理
→ base_ggmkdetail读取lx='cx'控件
→ 输入值进入@edit1～@edit7
→ 路由表查action='cx'
→ 调用TEST.dbo.p_equipment_cx
→ @ggmk_lx='cx'
→ 返回设备列表
```

------

## 2. 新增

```
用户点击新增
→ 打开lx='xz'控件窗口
→ 控件值进入@clyj1～@clyj9
→ 公共程序将@clyj设为“新增设备”
→ 路由表查action='cz'
→ 调用TEST.dbo.p_equipment_cz
→ IF @clyj='新增设备'
→ 插入主表
→ 写入操作记录表
```

------

## 3. 修改

```
用户选中一条设备
→ 右键“修改设备”
→ 当前设备编号进入@pkey1
→ 打开rid=修改菜单cid的lx='r'窗口
→ 修改字段进入@clyj1～@clyj9
→ @clyj='修改设备'
→ 路由action='cz'
→ 调用p_equipment_cz
→ 更新主表
→ 写入修改记录
```

------

## 4. 删除

```
用户选中一条设备
→ 右键“删除设备”
→ 当前设备编号进入@pkey1
→ 删除原因进入@clyj1
→ @clyj='删除设备'
→ 路由action='cz'
→ 调用p_equipment_cz
→ flag_zf改为1
→ 保存删除原因
→ 写入删除记录
```

------

## 5. 查看操作记录

```
用户选中设备
→ 右键“查看操作记录”
→ 当前设备编号进入@pkey1
→ 路由action='mx'
→ 调用p_equipment_cx
→ @ggmk_lx='mx'
→ 查询t_equipment_backup
```

------

## 6. 打开上下选

```
用户选中设备
→ 右键“设备标签管理”
→ base_ggmkright.lx=6
→ 打开form_common_comment
→ 当前行设备编号进入@args1
→ 路由查找“设备标签上下选”
→ cx调用标签查询过程
→ cz调用标签操作过程
```

------

# 第十五部分：最容易出错的地方

## 1. 查询使用 `@editN`，操作窗口使用 `@clyjN`

基于你截图中的实际系统：

```
查询条件 → edit
新增修改删除窗口 → clyjN
```

不能把新增字段写成：

```
@edit1
@edit2
```

因为页面实际传入的可能是：

```
@clyj1
@clyj2
```

------

## 2. `@clyj` 和 `@clyj1` 不是一回事

```
@clyj  = 当前操作名称，例如“修改设备”
@clyj1 = 修改窗口第一个字段值
```

例如：

```
IF @clyj = N'删除设备'
BEGIN
    -- @clyj1才是删除原因
END;
```

------

## 3. `xh=10` 不代表 `@clyj10`

新增窗口：

```
xh=10 → 第一个控件 → @clyj1
xh=20 → 第二个控件 → @clyj2
xh=30 → 第三个控件 → @clyj3
```

`xh` 只是显示顺序，不是参数号码。

------

## 4. 主键列名必须完全一致

以下三个地方必须统一：

```
base_ggmkmain.pkey     = 设备编号
base_ggmkright.pkey    = 设备编号
查询结果列名            = 设备编号
```

查询中必须有：

```
equipment_code AS [设备编号]
```

不能写成“设备编码”。

------

## 5. 修改、删除窗口的 `rid`

```
base_ggmkdetail.rid
=
base_ggmkright.cid
```

修改窗口不能随便写 `rid=0`。

------

## 6. 路由只负责找到过程

```
action='cx' → 查询过程
action='mx' → 明细或操作记录过程
action='cz' → 操作过程
```

真正判断新增、修改、删除的是：

```
@clyj
```

不是路由表中再分别配置三条 `add/update/delete`。

------

# 最终应形成的完整结构

```
TEST
├─ t_equipment
├─ t_equipment_backup
├─ p_equipment_cx
│  ├─ cx查询主页面
│  └─ mx查询操作记录
└─ p_equipment_cz
   ├─ 新增设备
   ├─ 修改设备
   └─ 删除设备

PUBLIC_CS
├─ base_ggmkmain
│  └─ 设备台账管理
├─ base_ggmkdetail
│  ├─ cx查询控件
│  ├─ xz新增控件
│  ├─ r修改控件
│  └─ r删除控件
├─ base_ggmkright
│  ├─ 查看操作记录
│  ├─ 修改设备
│  ├─ 删除设备
│  └─ 设备标签管理
└─ t_ggmk_routes
   ├─ cx → p_equipment_cx
   ├─ mx → p_equipment_cx
   └─ cz → p_equipment_cz
```

这套流程学会以后，开发图书、物资、车辆、人员、合同等页面时，只需要更换业务表字段和窗口控件，公共页面的开发结构基本完全相同。